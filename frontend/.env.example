# Leave blank for local development. Vite proxies /api to http://localhost:8000.
# For a separately hosted API, set the public API URL without a trailing slash.
# VITE_API_URL=https://your-api.example.com