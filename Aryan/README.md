# Aryan Contribution Area

Aryan owns the ESG intelligence application outside authentication. The runnable source remains in the shared monorepo so Vite and Express imports continue to work.

## Source ownership

- `frontend/src/pages/Dashboard.tsx`, `CompanyDetail.tsx`, `MatrixPage.tsx`, `DividendsPage.tsx`, `UploadPage.tsx`, and `PortfolioOptimizerPage.tsx`
- `frontend/src/components/` ESG, market, analytics, dashboard, sidebar, and notification components
- `frontend/src/data/`, `frontend/src/types/`, and non-auth API client methods
- `backend-node/src/routes/companies.js`, `dashboard.js`, `matrix.js`, `portfolio.js`, `alerts.js`, `apikeys.js`, `admin.js`, and `market.js`
- `backend-node/src/agents/`, `backend-node/src/services/`, `backend-node/src/scheduler.js`, and ESG-related Prisma models

## Evidence

The detailed submission material is in `docs/Kota Neil Aryan/` and `tests/Kota Neil Aryan/`.